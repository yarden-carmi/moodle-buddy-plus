- Save Moodle URL activities as `.txt` bookmarks containing the resolved external link, instead of failing with `SERVER_FAILED`
- Disambiguate duplicate course folders: when two courses share the same name within the same group, append the registrar's course number to each
- Show the "page not supported" view when the popup is opened on a non-Moodle tab (previously the popup hung on the loading spinner)
- Group courses into subfolders by their FCL group label, with a per-download toggle
- Add option to include already-seen resources when downloading a whole course
- Improve plugin-file folder path parsing and section-name retrieval
- Strengthen filename sanitization for cross-platform downloads
- Handle collapsed courses on the dashboard during scan and display
- Update extension and repository branding to Moodle Buddy+
